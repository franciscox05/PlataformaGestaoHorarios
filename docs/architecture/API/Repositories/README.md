# Repositories

Ponto de organizacao da API para persistencia.

Os repositories JPA concretos continuam em:

```text
src/main/java/com/example/projeto2/Repositories
```

Isto evita duplicar a camada de acesso a base de dados.
